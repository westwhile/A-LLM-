# 历史目录说明

仓库主线仅为 `A-LLM-`。工作区同级的 `A-LLM-data-engineering-publish`、`A-LLM-engineering-quality` 和 `A-LLM-portfolio-backtest-only` 是阶段性来源，已停止并行开发。

其中可复用的 LLM、质量门禁、数据工程和事件回测能力已经合并到主线。历史目录不应加入 `PYTHONPATH`，也不应作为运行入口；保留它们只用于追溯早期实现。

