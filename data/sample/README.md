# Sample Data

Run `python scripts/generate_sample_data.py` to create deterministic synthetic CSV data.

These files are only for pipeline validation. They are not real market data and must not be used for investment conclusions.
