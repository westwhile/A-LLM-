# 数据源审查：suspension（停复牌区间）

- 审查日期：2026-07-19
- 审查人：Kimi（起草，待用户签署确认）
- 审查结论：**保持阻断**（提供许可数据优先；可选推导方案待用户批准口径后方可启用）。

## 已做开源核验

- AkShare 1.18.64 无覆盖 2015 年至今的逐股停复牌历史接口（`news_trade_notify_suspend_baidu` 为新闻流，非结构化历史）。
- 各行情源在停牌日直接无 K 线行，因此存在推导可能（见下）。

## 需要用户提供的表（schema 不得更改）

```text
ts_code, suspend_date, resume_date
```

- 一次停牌区间一行（`ts_code + suspend_date` 主键）；`resume_date` 可空（长期停牌至今）
- 区间不得重叠（门禁硬检查）；覆盖 2015-01-01 起全部历史股票池成员

## 可接受来源

1. Wind / iFinD 停复牌历史导出
2. 交易所停复牌公告结构化数据（授权渠道）

## 可选推导方案（需用户书面批准口径）

从已落盘数据推导：`开市日 ∩ (list_date, delist_date) 内 且 当日无 daily_bar` 的日期计为停牌日，连续区间合并为一条记录。

- 优点：完全来自已审查批次，可复现。
- 风险：源数据本身的缺行（如腾讯源个别缺日）会被误判为停牌；无法区分"全天停牌"与"数据缺失"。
- 若启用，登记表 `source_type` 记为 `derived_from_approved_tables`，并在本文件追加推导规则版本与抽查核对记录（建议对 ≥20 只已知真实停牌股票做人工比对）。

在用户明确批准前，推导表只作为暂存证据，不进入登记表与导入。

---

## 2026-07-26 更新：用户提供 RESSET 许可数据（预检通过，无需推导）

### 实际来源

- 提供方：RESSET 金融研究数据库（中央财经大学图书馆订阅）；表 SUSPNSNRESMPTN（停复牌）
- 导出人：用户本人；导出日期：2026-07-26；日期范围：时间不限（2005-04-21 ~ 2026-07-01）
- 原始文件：`data/staging/real-pit-20260725/source/suspension.csv`（81,535 行 × 21 列，SHA-256 `169021b6…`）

### 预检结果

- 粒度：一次停牌事件一行，区间语义 `[停牌日期, 复牌日期)` 与模板"左闭右开"一致 ✓
- 类型分布：50(1天)=39,754、60(1小时)=19,874、30(连续)=19,451、40(盘中)=2,064、20/10 少量
- 日级过滤（保留 30/50）后：59,205 行，**主键重复 0、区间重叠 0** ✓
- `复牌日期=1900-01-01` 为"至今未复牌/停牌至退市"哨兵（339 行，均为连续停牌）→ 导入映射为空
- 史实抽查：信威集团 2016-12-27→2019-07-12 长停 ✓；乐视网停牌至退市 ✓
- 中证500 历史成员 1,356 只中 1,196 只有停牌记录

### 导入规则 v1（待用户签署确认）

- **F1**：保留 `SuspnsnType ∈ {30, 50}`（日级停牌）；剔除 10/20/40/60（盘中/1 小时，原始文件保留）
- **F2**：`ResmptnDt == 1900-01-01` → 空值（仍未复牌）
- **F3**：保留 `InfoPubDt`（信息发布日期）、`SuspnsnReason`、`SuspnsnType` 作为溯源列（模板允许增列）
- 原"行情缺行推导方案"不再启用（有直接来源）

### 2026-07-27 补充：科创板停复牌（主库表不含 688 的修补）

- 发现：主库 SUSPNSNRESMPTN 不含任何 688/689 记录（0 行）→ 从 RESSET 科创板库·科创板停复牌补导
- 文件：`source/suspension_kcb.csv`（562 行、192 只，事件表结构：HaltResuType 1=停牌/2=复牌，283/279 平衡，4 只停牌未复牌）
- 104 只科创成员中 29 只有停牌记录，其余为上市至今未停牌（合理——科创板 2019 年才开市且停牌少于主板）
- 规则 v2（待批准）：KCB 事件按 HaltResuType 配对成 `[停牌日, 复牌日)` 区间后并入主表
